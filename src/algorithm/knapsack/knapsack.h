#pragma once

namespace pccb {
  long long solve_large_knapsack(
      const int num_item,
      long long weights[],
      const long long values[],
      const long long weight);
} // namespace pccb
