#pragma once
#include <vector>

namespace algorithm {
  std::vector<int> SolveKThNumber(
      const int num_seq,
      int seq[],
      const std::vector<std::vector<int>>& queries);
} // namespace algorithm
