#pragma once

/**
 * @brief 
 *
 * @param data[] permutation of sequence from 1 to n
 * @param size
 *
 * @return num of swaps when do bubble sort
 */
long long int count_bubble_sort(int data[], const int size);
